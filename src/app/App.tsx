import { Header } from "../widgets/Header";
import styles from "./App.module.css";

function App() {
    return (
        <main className={styles.App}>
            <Header />
        </main>
    );
}

export default App;
